import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#5636D3',
      light: '#775edb',
      dark: '#3c2593'
    },
    secondary: {
      main: '#FF872C',
      light: '#FF9F56',
      dark: '#B25E1E'
    },
    background: {
      paper: '#F0F0F0'
    }
  }
});

export default theme;
